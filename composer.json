{
  "name": "yourorg/clean-mvc-skeleton",
  "description": "Clean Architecture MVC skeleton (Framework + App layers)",
  "type": "project",
  "require": {
    "php": ">=8.1",
    "phpmailer/phpmailer": "^7.0",
    "aws/aws-sdk-php": "^3.359",
    "php-amqplib/php-amqplib": "^3.0",
    "nmred/kafka-php": "^0.2.0",
    "elasticsearch/elasticsearch": "^8.13"
  },
  "require-dev": {
    "jetbrains/phpstorm-stubs": "*"
  },
  "suggest": {
    "ext-redis": "Required for Redis cache and queue drivers",
    "ext-pdo_mysql": "Required for MySQL database support",
    "ext-pdo_pgsql": "Required for PostgreSQL database support",
    "php-amqplib/php-amqplib": "Required for RabbitMQ queue driver",
    "enqueue/rdkafka": "Required for Kafka realtime broker (high performance, requires librdkafka extension)",
    "nmred/kafka-php": "Alternative Kafka client library (pure PHP, no C extension required)"
  },
  "autoload": {
    "psr-4": {
      "Toporia\\Framework\\": "src/Framework/",
      "App\\": "src/App/"
    },
    "files": [
      "src/Framework/Support/helpers.php"
    ]
  }
}
